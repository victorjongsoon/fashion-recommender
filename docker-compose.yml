services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: inventory
      POSTGRES_USER: inventory
      POSTGRES_PASSWORD: inventory
    volumes:
      - ./db/01_init.sql:/docker-entrypoint-initdb.d/01_init.sql
      - ./db/02_load_hnm.sql:/docker-entrypoint-initdb.d/02_load.sql
      - ./db/03_compute_dead_stock.sql:/docker-entrypoint-initdb.d/03_compute_dead_stock.sql
      - ./data/raw/hnm:/data/hnm:ro
    ports:
      - "5432:5432"

  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"

  image-service:
    build:
      context: ./services/image-service
    environment:
      IMAGES_DIR: /data/images
    volumes:
      - ./data/raw/hnm/images:/data/images:ro
    ports:
      - "8001:8000"

  vton-service:
    build: ./services/vton-services
    ports:
      - "8002:8000"
    environment:
      HF_TOKEN: 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
